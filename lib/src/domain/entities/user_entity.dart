class UserEntity {
  final String id;      // uid
  final String email;   // email
  final String name;    // displayName

  const UserEntity({
    required this.id,
    required this.email,
    required this.name,
  });
}
