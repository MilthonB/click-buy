import 'package:flutter/material.dart';

class NotfoundScreens extends StatelessWidget {
  const NotfoundScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset(
        'assets/notfound/404.png'
      )
    );
  }
}
